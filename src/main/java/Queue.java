/*
import java.sql.Array;
import java.util.ArrayList;
import java.util.Deque;
import java.util.LinkedList;
import java.util.List;

public class Queue {
    private final List<Patient> patients =  new ArrayList<>();
    
    public Queue() {}

    public boolean isEmpty() {
        return patients.isEmpty();
    }

    public void add(Patient patient) {
        patients.add(patient);
    }


    public Patient remove() {
        if (patients.isEmpty()) {
            throw new EmptyQueueException();
        }

        Patient firstPatient = patients.get(0);
        patients.remove(firstPatient);

        return firstPatient;
    }

    public Patient peek() {
        return patients.get(0);
    }

    public Patient peekAtIndex(int index) {
        return patients.get(index);
    }

    public void pushFront(Patient patient){
        List<Patient> newPatientList = new ArrayList();

        newPatientList.add(patient);
        newPatientList.(patiens);

    }
}
*/
